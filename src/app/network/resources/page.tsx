import { NetworkLabWorkspace } from "@/components/labs/network/NetworkLabWorkspace";

export default function NetworkResourcesPage() {
  return <NetworkLabWorkspace initialTopicId="network-resources" />;
}
